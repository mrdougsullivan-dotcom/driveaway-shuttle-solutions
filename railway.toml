[build]
builder = "NIXPACKS"
buildCommand = "cd backend && bun install && bunx prisma generate"

[deploy]
startCommand = "cd backend && bunx prisma migrate deploy && bun run src/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "3000"
NODE_ENV = "production"
